version: v2
lint:
  use:
    - STANDARD
  except:
    - RPC_RESPONSE_STANDARD_NAME
    - RPC_REQUEST_RESPONSE_UNIQUE
    - ENUM_ZERO_VALUE_SUFFIX      # Отключает требование _UNSPECIFIED для нулевого значения
    - ENUM_VALUE_PREFIX           # Отключает требование префикса имени enum